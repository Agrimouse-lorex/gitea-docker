version: '3.8'
services:
  app:
    image: node:20
    working_dir: /usr/src/app
    volumes:
      - ./:/usr/src/app:cached
      - /usr/src/app/node_modules
    ports:
      - "3000:3000"
    command: bash -lc "npm install && npm run dev"
  # Якщо потрібна БД, додай тут postgres/mysql і зв'яжи
